/**
 * Reune las excepciones propias del dominio de usuarios y permisos.
 *
 * <p>Estas excepciones se lanzan cuando una operacion requiere un tipo de
 * usuario o privilegio que el solicitante no posee.</p>
 */
package excepciones;
